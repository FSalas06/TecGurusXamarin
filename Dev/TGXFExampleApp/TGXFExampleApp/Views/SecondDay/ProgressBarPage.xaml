<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="TGXFExampleApp.Views.SecondDay.ProgressBarPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <ContentPage.Content>
        <StackLayout
            Padding="20"
            Spacing="20">
            <StackLayout Spacing="10">
                <ProgressBar
                    Progress="{Binding Bar1Prog}"
                    ProgressColor="Red" />
                <Button
                    Command="{Binding ProgressCommand}"
                    Text="Add more!" />
            </StackLayout>

            <StackLayout Spacing="10">
                <ProgressBar
                    Progress="{Binding Bar2Prog}"
                    ProgressColor="Red" />
                <Label Text="{Binding Progress2Text}" />
            </StackLayout>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>